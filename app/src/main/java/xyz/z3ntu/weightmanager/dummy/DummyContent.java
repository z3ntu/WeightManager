/*
package xyz.z3ntu.weightmanager.dummy;

import java.util.ArrayList;
import java.util.Calendar;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import xyz.z3ntu.weightmanager.DateWeightEntry;

*/
/**
 * Helper class for providing sample content for user interfaces created by
 * Android template wizards.
 * <p>
 * TODO: Replace all uses of this class before publishing your app.
 *//*

public class DummyContent {

    */
/**
     * An array of sample (dummy) items.
     *//*

    public static final List<DateWeightEntry> ITEMS = new ArrayList<>();

    */
/**
     * A map of sample (dummy) items, by ID.
     *//*

    public static final Map<String, DateWeightEntry> ITEM_MAP = new HashMap<>();

    private static final int COUNT = 50;

    static {
        // Add some sample items.
        for (int i = 1; i <= COUNT; i++) {
            addItem(createDummyItem(i));
        }
    }

    private static void addItem(DateWeightEntry item) {
        ITEMS.add(item);
        ITEM_MAP.put(item.getDate(), item);
    }

    private static DateWeightEntry createDummyItem(int position) {
        return new DateWeightEntry(Calendar.getInstance().getTime(), position);
    }
}
*/
